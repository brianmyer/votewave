<h2>Latest Polls</h2>

{{#each polls as |poll| }}
<a href="/results/{{poll.id}}">
  <section>
    <h3>{{poll.name}}</h3>
    <div id="poll-{{poll.id}}"></div>
    <p>Poll created by {{poll.user.name}} </p>
  </section>
</a>
{{/each}}
<!-- Limit to 4 or 5? -->