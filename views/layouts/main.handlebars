<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>VoteWave</title>
  <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
  <link rel="stylesheet" href="/css/normalize.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-gray-600">

  <header class="bg-gray-800">

    <h1 class="text-3xl md:text-6xl text-center p-4 text-blue-100"><a href="/">VoteWave</a></h1>

    <nav class="hidden md:block absolute top-4 right-4">
      {{#if logged_in}}
      <button class="text-white"><a href="/dashboard"><img class="w-16" src="/images/profile.png"></a></button>
      {{else}}
      <button class="text-white"><a href="/login"><img class="w-16" src="/images/profile.png"></a></button>
      {{/if}}
    </nav>

  </header>

  {{!-- mobile nav --}}
  <nav class="fixed bottom-0 left-0 right-0 h-20 w-full bg-gray-800 md:hidden flex justify-evenly">

    <button class="text-white"><a href="/"><img class="w-16" src="/images/home.png"></a></button>

    {{#if logged_in}}
      <button class="text-white"><a href="/dashboard"><img class="w-16" src="/images/profile.png"></a></button>
    {{else}}
      <button class="text-white"><a href="/login"><img class="w-16" src="/images/profile.png"></a></button>
    {{/if}}

  </nav>


  {{!-- main content --}}
  <main>
    {{{ body }}}

    {{!-- modals --}}
    <div class="hidden" id="modals">

      {{!-- success --}}
      <div id="vote-success" class="opacity-0 transition-opacity duration-100 top-40 right-10 fixed bg-green-400 bg-opacity-50 p-4 rounded-3xl">
        <h2 class="text-2xl mb-4">Success</h2>
        <p>Thank You for your response!</p>
      </div>

      {{!-- errors --}}
      <div id="login-error" class="opacity-0 transition-opacity duration-100 top-40 right-10 fixed bg-red-400 bg-opacity-50 p-4 rounded-3xl">
        <h2 class="text-2xl mb-4">Error</h2>
        <p>Failed to Login. Please try again.</p>
      </div>
      <div id="signup-error" class="opacity-0 transition-opacity duration-100 top-40 right-10 fixed bg-red-400 bg-opacity-50 p-4 rounded-3xl">
        <h2 class="text-2xl mb-4">Error</h2>
        <p>Failed to Sign Up. Please try again.</p>
      </div>
      <div id="create-poll-error" class="opacity-0 transition-opacity duration-100 top-40 right-10 fixed bg-red-400 bg-opacity-50 p-4 rounded-3xl">
        <h2 class="text-2xl mb-4">Error</h2>
        <p>Failed to Create Poll. Please try again.</p>
      </div>
      <div id="create-responses-error" class="opacity-0 transition-opacity duration-100 top-40 right-10 fixed bg-red-400 bg-opacity-50 p-4 rounded-3xl">
        <h2 class="text-2xl mb-4">Error</h2>
        <p>Failed to Create Responses. Please try again.</p>
      </div>
      <div id="logout-error" class="opacity-0 transition-opacity duration-100 top-40 right-10 fixed bg-red-400 bg-opacity-50 p-4 rounded-3xl">
        <h2 class="text-2xl mb-4">Error</h2>
        <p>Failed to Logout. Please try again.</p>
      </div>

    </div>

    {{!-- background credit to https://css-generators.com/wavy-shapes/ for the wave generation --}}
    <div class="absolute bottom-0 bg-blue-300 w-full h-2/3" style="--mask: radial-gradient(201.25px at 50% 270.00px,#000 99%,#0000 101%) calc(50% - 180px) 0/360px 100%, radial-gradient(201.25px at 50% -180px,#0000 99%,#000 101%) 50% 90px/360px 100% repeat-x; -webkit-mask: var(--mask); mask: var(--mask); z-index: -1;"></div>
    <div class="absolute bottom-0 bg-blue-400 w-full h-1/2" style="--mask: radial-gradient(201.25px at 50% 270.00px,#000 99%,#0000 101%) calc(50% - 180px) 0/360px 100%, radial-gradient(201.25px at 50% -180px,#0000 99%,#000 101%) 50% 90px/360px 100% repeat-x; -webkit-mask: var(--mask); mask: var(--mask); z-index: -1;"></div>
    <div class="absolute bottom-0 bg-blue-500 w-full h-1/3" style="--mask: radial-gradient(201.25px at 50% 270.00px,#000 99%,#0000 101%) calc(50% - 180px) 0/360px 100%, radial-gradient(201.25px at 50% -180px,#0000 99%,#000 101%) 50% 90px/360px 100% repeat-x; -webkit-mask: var(--mask); mask: var(--mask); z-index: -1;"></div>

  </main>

  <footer>

    <p class="text-center">&#169 2023 Cameron Hack, Brian Myer & Christian Dziedzic</p>

  </footer>

</body>
</html>